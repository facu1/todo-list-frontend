export enum PathsOfRoutes {
  AUTH = "/",
  HOME = "/home",
  REPORT = "/report",
  NOTFOUND = "/:pathMatch(.*)*",
}

export enum IconNames {
  LOADING = "loading",
  MENU = "menu",
  CALENDAR = "calendar",
  CALENDARCHECK = "calendarCheck",
  CHECK = "check",
  TRASH = "trash",
  PLUS = "plus",
  HOME = "home",
  REPORT = "report",
  ARROWDOWN = "arrowDown",
}
